# Helper compatibility notes

No helper package has been published yet. Replace the manifest owner, versions, asset URLs, and SHA-256 values only after each repackaged helper passes parser and smoke tests.

Upstream components remain subject to their own licences. Place redistributed licence notices in `helpers/licences/` before publishing helper assets.
